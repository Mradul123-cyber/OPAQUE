<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Allow cleartext (HTTP) traffic for local development servers.
        Production traffic (HTTPS) is already secure and unaffected.
        Add or change IP addresses below when your dev machine IP changes.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Local dev server -->
        <domain includeSubdomains="false">192.168.29.81</domain>
        <!-- Add other local IPs here if needed, e.g. emulator host -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>

    <!-- All other connections (production) must use HTTPS -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
